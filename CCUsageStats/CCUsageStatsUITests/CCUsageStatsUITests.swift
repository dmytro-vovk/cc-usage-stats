import XCTest

/// Placeholder. CCUsageStats is a menubar-only LSUIElement app with no
/// hostable window, so XCUIApplication snapshot-based tests cannot run
/// against it. Use `xcodebuild test ... -only-testing:CCUsageStatsTests`
/// to run the actual unit suite.
final class CCUsageStatsUITests: XCTestCase {
    func testNoOp() {
        // Intentionally empty.
    }
}
